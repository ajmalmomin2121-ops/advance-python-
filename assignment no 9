import csv
import json
import os

def read_csv(input_path):
    with open(input_path, "r", newline="") as f:
        reader = csv.DictReader(f)
        return list(reader)

def write_json(output_path, data):
    with open(output_path, "w") as f:
        json.dump(data, f, indent=4)

def convert_csv_to_json(input_path, output_path):
    data = read_csv(input_path)
    write_json(output_path, data)
    return data

if __name__ == "__main__":
    base_dir = os.path.dirname(os.path.abspath(__file__))
    input_path = os.path.join(base_dir, "students.csv")
    output_path = os.path.join(base_dir, "students.json")

    sample_csv = (
        "id,name,department,marks\n"
        "1,Aditi Sharma,Computer Science,88\n"
        "2,Rahul Verma,Mechanical,76\n"
        "3,Sneha Iyer,Electronics,92\n"
    )

    with open(input_path, "w", newline="") as f:
        f.write(sample_csv)

    print(f"Created sample CSV file: {input_path}\n")

    print(f"Contents of '{input_path}':")
    with open(input_path, "r") as f:
        print(f.read())

    data = convert_csv_to_json(input_path, output_path)
    print(f"Converted {len(data)} row(s) from CSV to JSON.")
    print(f"JSON written to: {output_path}\n")

    print(f"Contents of '{output_path}':")
    with open(output_path, "r") as f:
        print(f.read())


output 
_________________________________________________________________________________________________________________
ajmal@Mac / % /usr/local/bin/python3 /Users/ajmal/Documents/assign8.py
Created sample CSV file: /Users/ajmal/Documents/students.csv

Contents of '/Users/ajmal/Documents/students.csv':
id,name,department,marks
1,Aditi Sharma,Computer Science,88
2,Rahul Verma,Mechanical,76
3,Sneha Iyer,Electronics,92

Converted 3 row(s) from CSV to JSON.
JSON written to: /Users/ajmal/Documents/students.json

Contents of '/Users/ajmal/Documents/students.json':
[
    {
        "id": "1",
        "name": "Aditi Sharma",
        "department": "Computer Science",
        "marks": "88"
    },
    {
        "id": "2",
        "name": "Rahul Verma",
        "department": "Mechanical",
        "marks": "76"
    },
    {
        "id": "3",
        "name": "Sneha Iyer",
        "department": "Electronics",
        "marks": "92"
    }
]
